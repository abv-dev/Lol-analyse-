/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tout le contenu est statique (SSG) : les études sont des fichiers,
  // il n'y a aucun backend.
  reactStrictMode: true,
};

export default nextConfig;
